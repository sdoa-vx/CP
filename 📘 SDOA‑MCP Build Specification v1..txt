📘 SDOA‑MCP Build Specification v1.0
Authoritative Implementation Plan  
Project Root: C:\MCP  
Sovereign: SDOA-MCP-server  
Date: 2026‑06‑18
Status: ACTIVE BUILD

***note read "c:\MCP\documentation" for context, but do not include it in the build 

1. PROJECT ROOT STRUCTURE (CURRENT)
Code
SDOA-MCP-server/
  package.json
  package-lock.json

  migrations/
    init-schema.js

  graph/
    engine.js

  authorities/
    mcp/
      PortfolioManager.service.js
      server.js
This is correct and SDOA‑compliant so far.

2. WHAT IS COMPLETE
✔ Node environment
ESM enabled

Correct dependencies installed

SQLite engine available

Graphology engine available

Zod validation available

MCP SDK available

✔ Core directories created
authorities/mcp/

graph/

migrations/

✔ SQLite schema bootstrap
init-schema.js exists

Core tables created

✔ Portfolio Manager
Local + remote hybrid portfolio loader

Git sync support

Manifest extraction

Portfolio → SQLite ingestion

Portfolio update tracking

✔ MCP bootstrap
Stdio server

SQLite wiring

Portfolio wiring

Graph engine wiring

First tool stub (sdoa.analyze)

3. WHAT IS STILL REQUIRED (AUTHORITATIVE LIST)
This is the exact remaining work to complete the SDOA‑MCP server.

3.1 Canonical SDOA Portfolio (Local)
Location:

Code
C:\MCP\authorities\sdoa-portfolio\
Must contain:

Code
primitives/
features/
adapters/
services/
workflows/
engines/
schemas/
Each module must include:

*.prim.js, *.feature.js, etc

Full SDOA v5.0 MANIFEST

Co-located CSS (for UI primitives/features)

Co-located schemas (for features)

Purpose:  
Used by REUSE + INNOVATE phases to detect reusable modules and patterns.

3.2 Canonical SDOA Portfolio (Remote GitHub Repo)
Repo name: sdoa-portfolio  
Structure: identical to local portfolio
Used for:

Syncing updates

Community contributions

Versioned canonical library

PortfolioManager already supports:

clone

pull

merge

3.3 Core MCP Services (Phase 1)
These must be created under:

Code
authorities/mcp/
1. ConstraintSolver.service.js
Implements ALL SDOA governance rules:

Sovereignty

Manifest completeness

Layer rules

Naming conventions

Placement rules

Line limits

Lifecycle contracts

Deduplication

2. RunManager.service.js
Handles:

run creation

phase transitions

run status

run resume/reset

writing to phases table

writing to run_log

3. LlmBroker.adapter.js
Routes phases → Ollama models:

llama3.2

codellama

llama3:8b

nomic-embed-text

4. EmbeddingService.js
Wraps nomic‑embed‑text:

generate embeddings

store in SQLite BLOB

cosine similarity

3.4 Graph Engine Integration (Phase 2)
graph/engine.js exists but must be extended:

boundary violation detection

layer violation detection

sovereignty violation detection

graph cache writer

graph integrity validator

3.5 Pipeline State Machine (Phase 3)
PipelineStateMachine.js must enforce:

ANALYZE → MAP → REUSE → INNOVATE → PLAN → PATCH → AUDIT → MIGRATE → VERIFY

MIGRATE blocked unless AUDIT passes

resume logic

reset logic

3.6 Workflow Modules (Phase 4)
Each workflow is a sovereign module:

Code
authorities/mcp/workflows/
  Analyze.workflow.js
  Map.workflow.js
  Reuse.workflow.js
  Innovate.workflow.js
  Plan.workflow.js
  Patch.workflow.js
  Audit.workflow.js
  Migrate.workflow.js
  Verify.workflow.js
Each workflow must:

Validate input with Zod

Load run state

Call LLMs (via LlmBroker)

Call ConstraintSolver

Call Graph engine

Write results to SQLite

Return MCP tool output

3.7 MCP Tool Definitions (Phase 5)
In server.js or SdoaMcp.service.js, register tools:

Code
sdoa.analyze
sdoa.map
sdoa.reuse
sdoa.innovate
sdoa.plan
sdoa.patch
sdoa.audit
sdoa.migrate
sdoa.verify

sdoa.run.list
sdoa.run.status
sdoa.run.resume
sdoa.run.reset
Each tool:

Zod input schema

Zod output schema

Calls workflow

Writes to SQLite

3.8 VS Code / Antigravity Extension (Phase 6)
Directory:

Code
authorities/mcp/vscode-extension/
Files:

package.json (extension manifest)

extension.ts (activation + MCP bridge)

Providers:

SdoaCodeLens.ts

SdoaDiagnostics.ts

SdoaTreeView.ts

Webview:

PipelineDashboard.html

3.9 Integration Test (Phase 7)
Run:

Code
sdoa.analyze({ path: "C:/SDOAvX" })
sdoa.map
sdoa.reuse
sdoa.innovate
sdoa.plan
sdoa.patch
sdoa.audit
sdoa.migrate
sdoa.verify
Ensure:

No blockers

Graph integrity

SDOA compliance

Portfolio updates recorded

4. BUILD ORDER (AUTHORITATIVE)
Phase 0 — Bootstrap (DONE)
✔ package.json
✔ SQLite schema
✔ PortfolioManager
✔ server.js bootstrap

Phase 1 — Core Services (NEXT)
ConstraintSolver.service.js

RunManager.service.js

LlmBroker.adapter.js

EmbeddingService.js

Phase 2 — Graph Engine Enhancements
boundary rules

layer rules

sovereignty rules

graph cache

Phase 3 — Pipeline State Machine
transitions

resume/reset

enforcement

Phase 4 — Workflow Modules
9 workflows

Zod schemas

SQLite writes

LLM calls

Phase 5 — MCP Tool Registration
sdoa.* tools

Phase 6 — VS Code / Antigravity Extension
diagnostics

codelens

tree view

dashboard

Phase 7 — Integration Test
full pipeline

self‑audit

portfolio sync

5. Full project skeleton 

SDOA-MCP-server = "C:\MCP"

SDOA-MCP-server/
  package.json
  package-lock.json
  server.js

  migrations/
    init-schema.js

  graph/
    engine.js

  authorities/
    mcp/
      PortfolioManager.service.js
      SdoaMcp.service.js        (later: full MCP entry if you want separate)
      CapabilityGraph.engine.js (optional alias to graph/engine.js)
      PipelineStateMachine.js   (TBD)
      ConstraintSolver.service.js (TBD)
      RunManager.service.js     (TBD)
      LlmBroker.adapter.js      (TBD)
      EmbeddingService.js       (TBD)
      manifest.json             (TBD)
      workflows/
        Analyze.workflow.js     (TBD)
        Map.workflow.js         (TBD)
        Reuse.workflow.js       (TBD)
        Innovate.workflow.js    (TBD)
        Plan.workflow.js        (TBD)
        Patch.workflow.js       (TBD)
        Audit.workflow.js       (TBD)
        Migrate.workflow.js     (TBD)
        Verify.workflow.js      (TBD)

  authorities/
    sdoa-portfolio/
      primitives/               (canonical primitives)
      features/
      adapters/
      services/
      workflows/
      engines/
      schemas/

  environment/
    mcp.json
    sdoa.constraints.json
    .sdoa/
      config.json
      pipeline.db
      cache/
        graph-cache.json

6. WHAT YOU SHOULD DO NEXT
You now have the authoritative build spec.


The next step is:

Generate Phase 1 — Core Services
Starting with:

1. ConstraintSolver.service.js