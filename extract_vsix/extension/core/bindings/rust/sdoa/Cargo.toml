[package]
name = "sdoa"
version = "0.1.0"
edition = "2021"
description = "Safe Rust binding for the SDOA Engine (hybrid capability model)"

[dependencies]
sdoa-sys = { path = "../sdoa-sys" }
serde_json = "1"

[[example]]
name = "quickstart"
